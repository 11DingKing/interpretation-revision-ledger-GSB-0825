package com.ledger.ril.repo;

import com.ledger.ril.domain.InterpretationThread;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InterpretationThreadRepository extends JpaRepository<InterpretationThread, String> {
}
